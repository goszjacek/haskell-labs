-- co to jest akcja IO?
-- co to jest funktor?
-- co to jest funktor aplikatywny?
-- funkcje z pierszego zadania jak putstr nie sa wazne
-- notacja do zamiast potokow
-- operator then
-- przetlumaczyc do notacji do
-- na kartktowce nie bedzie plikow ani obslugi wyjatkow
-- pure umieszcza w kontekscie funktora aplikatywnego
-- $ raz przenosi do kontestu, potem trzeba *

-- getChar?
-- putChar?
-- getline?
-- print?
-- czy print dziala na po prostu stworzonych newtypach?
-- Monad? 
-- czy hello to bez cudzyslowania to return ? 
-- jakie typu jest po prostu jakies wczesniej niedeklarowane :t hello ? 
-- czy zadziala po prostu show hello [nie]

-- definicja algebraicznego typu danych: 
-- mamy pewne podstawowe typy danych. Na poziomie algebraicznego typu danych mozemy tworzyc nowe typy w oparci o zbiorowa sume lub iloczyn

